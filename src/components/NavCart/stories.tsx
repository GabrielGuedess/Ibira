import { Story, Meta } from '@storybook/react/types-6-0';
import NavCart from '.';

export default {
  title: 'NavCart',
  component: NavCart,
} as Meta;

export const Default: Story = () => <NavCart />;
