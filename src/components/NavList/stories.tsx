import { Story, Meta } from '@storybook/react/types-6-0';
import NavList from '.';

export default {
  title: 'NavList',
  component: NavList,
} as Meta;

export const Default: Story = () => <NavList />;
