import { Story, Meta } from '@storybook/react/types-6-0';
import NavSearch from '.';

export default {
  title: 'NavSearch',
  component: NavSearch,
} as Meta;

export const Default: Story = () => <NavSearch />;
