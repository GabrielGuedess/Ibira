import { Story, Meta } from '@storybook/react/types-6-0';
import CloseX from '.';

export default {
  title: 'CloseX',
  component: CloseX,
} as Meta;

export const Default: Story = () => <CloseX />;
