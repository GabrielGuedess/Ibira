import { Story, Meta } from '@storybook/react/types-6-0';
import MenuModal from '.';

export default {
  title: 'MenuModal',
  component: MenuModal,
} as Meta;

export const Default: Story = () => <MenuModal />;
