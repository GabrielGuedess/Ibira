import { Story, Meta } from '@storybook/react/types-6-0';
import NavUser from '.';

export default {
  title: 'NavUser',
  component: NavUser,
} as Meta;

export const Default: Story = () => <NavUser />;
